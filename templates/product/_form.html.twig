
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_row(form.name) }}
    {{ form_row(form.price) }}
    {{ form_row(form.save, { 'label': 'Save'}) }}

    {{ form_end(form) }}